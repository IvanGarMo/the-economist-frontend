import { Injectable } from '@angular/core';
import { Login } from '../_models/Login';

@Injectable({
  providedIn: 'root'
})
export class LoginService {

  constructor() { }

  login(login: Login) {
    
  }
}
